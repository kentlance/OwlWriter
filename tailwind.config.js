/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html", // Look in main HTML file
    "./scripts/**/*.js", // Look in all JavaScript files inside  scripts folder
    // can add more if needed
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
